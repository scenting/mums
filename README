# Vas a necesitar docker y docker-compose

# Para construir el entorno
docker-compose build

# Migrar la BBDD
docker-compose run --rm web ./manage.py migrate

# Arrancamos el entorno, se arrancara en el puerto 8000 (fallara si ya esta
# vinculado)
docker-compose up

Probar borrando el volumen, igual no espera a que este levantado

# Para ejecutar los tests
docker-compose run --rm web ./manage.py test --settings=mums.settings.test
